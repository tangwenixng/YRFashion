<view class="page">
  <block wx:if="{{loading}}">
    <view class="card skeleton"></view>
    <view class="card skeleton"></view>
  </block>

  <block wx:elif="{{error}}">
    <view class="card empty-card">
      <text class="empty-title">{{error}}</text>
      <button class="secondary-button" bindtap="loadProduct">Reload</button>
    </view>
  </block>

  <block wx:elif="{{product}}">
    <view class="hero">
      <text class="title">{{product.name}}</text>
      <text class="subtitle">{{product.images.length}} image(s)</text>
    </view>

    <view class="toolbar">
      <button class="primary-button" loading="{{uploading}}" bindtap="chooseImages">Upload Images</button>
      <button class="secondary-button" loading="{{savingSort}}" bindtap="saveSorts">Save Sort</button>
    </view>

    <block wx:if="{{product.images.length}}">
      <view class="list">
        <block wx:for="{{product.images}}" wx:key="id">
          <view class="card image-card">
            <image class="image" src="{{item.image_url}}" mode="aspectFill"></image>

            <view class="content">
              <view class="row">
                <text class="image-name">{{item.original_name}}</text>
                <text wx:if="{{item.is_cover}}" class="cover-badge">Cover</text>
              </view>

              <view class="sort-row">
                <text class="meta-label">Sort</text>
                <input
                  class="sort-input"
                  type="number"
                  value="{{item.sort_order}}"
                  data-image-id="{{item.id}}"
                  bindinput="handleSortInput"
                />
              </view>

              <view class="action-row">
                <button class="mini-button" data-image-id="{{item.id}}" bindtap="setCover">Set Cover</button>
                <button class="mini-button danger" data-image-id="{{item.id}}" bindtap="removeImage">Delete</button>
              </view>
            </view>
          </view>
        </block>
      </view>
    </block>

    <view wx:else class="card empty-card">
      <text class="empty-title">No images yet</text>
      <text class="empty-subtitle">Upload 1-3 images and pick a cover.</text>
    </view>
  </block>
</view>
