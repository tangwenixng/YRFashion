<view class="page">
  <view class="nav-shell" style="height: {{navBarTotalHeight}}px; padding-top: {{statusBarHeight}}px;">
    <view class="nav-content" style="height: {{navBarHeight}}px;">
      <view class="nav-left">
        <view class="nav-icon-button" bindtap="goBack">
          <view class="nav-back-icon"></view>
        </view>
        <view class="nav-icon-button" bindtap="goHome">
          <view class="nav-home-icon">
            <view class="nav-home-roof"></view>
            <view class="nav-home-body"></view>
            <view class="nav-home-door"></view>
          </view>
        </view>
      </view>
      <text class="nav-title">{{navTitle}}</text>
      <view class="nav-right-space" style="width: {{navRightSpaceWidth}}px;"></view>
    </view>
  </view>
  <view class="nav-placeholder" style="height: {{navBarTotalHeight}}px;"></view>

  <view class="page-header">
    <text class="title">穿搭分享</text>
    <text class="subtitle">浏览近期整理的搭配灵感</text>
  </view>

  <view class="search-bar">
    <view class="search-inline">
      <input
        class="search-input"
        placeholder="搜索穿搭名称或关键词"
        confirm-type="search"
        value="{{draftKeyword}}"
        bindinput="onKeywordInput"
        bindconfirm="submitSearch"
      />
    </view>
    <view wx:if="{{keyword || draftKeyword}}" class="search-state-bar">
      <text class="search-reset" bindtap="clearSearch">清空</text>
    </view>
  </view>

  <scroll-view wx:if="{{categories.length}}" class="category-strip" scroll-x enable-flex show-scrollbar="{{false}}">
    <view
      wx:for="{{categories}}"
      wx:key="id"
      class="category-chip {{item.id === activeCategoryId ? 'category-chip-active' : ''}}"
      data-category-id="{{item.id}}"
      bindtap="selectCategory"
    >
      {{item.name}}
    </view>
  </scroll-view>

  <block wx:if="{{loading}}">
    <view class="card skeleton"></view>
    <view class="card skeleton"></view>
  </block>

  <block wx:elif="{{error && !items.length}}">
    <view class="card empty-card">
      <text class="empty-title">{{error}}</text>
      <button class="primary-button" bindtap="reloadProducts">重新加载</button>
    </view>
  </block>

  <block wx:elif="{{items.length}}">
    <view class="list">
      <block wx:for="{{productColumns}}" wx:key="index" wx:for-item="column">
        <view class="product-column">
          <block wx:for="{{column}}" wx:key="id" wx:for-item="product">
            <view class="card product-card" bindtap="goToDetail" data-product-id="{{product.id}}">
              <image wx:if="{{product.cover_image_url}}" class="cover" src="{{product.cover_image_url}}" mode="widthFix"></image>
              <view wx:else class="cover cover-placeholder">暂无封面</view>
              <view class="content">
                <text class="product-name">{{product.name}}</text>
                <view class="tag-row">
                  <text wx:for="{{product.tags}}" wx:key="*this" class="tag">{{item}}</text>
                </view>
              </view>
            </view>
          </block>
        </view>
      </block>
    </view>
    <view class="footer-text">{{hasMore ? (loadingMore ? '加载更多中...' : '上滑继续查看') : '已经到底了'}}</view>
  </block>

  <block wx:else>
    <view class="card empty-card">
      <text class="empty-title">{{keyword ? '没有找到匹配的穿搭' : (activeCategoryId ? '当前分类下还没有相关穿搭' : '当前还没有可浏览的穿搭')}}</text>
      <text class="empty-subtitle">{{keyword ? '换个关键词，或者切换分类再试试' : (activeCategoryId ? '试试切换其他分类看看' : '整理完成后会在这里更新')}}</text>
    </view>
  </block>
</view>
