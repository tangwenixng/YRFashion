.page {
  min-height: 100vh;
  padding-bottom: 40rpx;
  background: linear-gradient(180deg, #fcf7f1 0%, #eee3d6 100%);
}

.hero {
  width: 100%;
  height: 720rpx;
  background: rgba(255, 255, 255, 0.65);
}

.hero-image,
.hero-placeholder {
  width: 100%;
  height: 100%;
}

.hero-placeholder {
  display: flex;
  align-items: center;
  justify-content: center;
  color: #8f7463;
}

.panel {
  margin: 24rpx 32rpx 0;
  padding: 30rpx;
  border-radius: 28rpx;
  background: rgba(255, 255, 255, 0.9);
  box-shadow: 0 18rpx 28rpx rgba(90, 60, 47, 0.08);
}

.title {
  display: block;
  font-size: 40rpx;
  font-weight: 700;
  color: #34231a;
}

.tag-row {
  margin-top: 18rpx;
  display: flex;
  flex-wrap: wrap;
  gap: 12rpx;
}

.tag {
  padding: 8rpx 16rpx;
  border-radius: 999px;
  background: #f0dfd1;
  color: #7b5d4c;
  font-size: 22rpx;
}

.description {
  display: block;
  margin-top: 24rpx;
  line-height: 1.9;
  color: #493327;
}

.action-bar {
  margin: 24rpx 32rpx 0;
  display: flex;
  flex-direction: column;
  gap: 18rpx;
}

.primary-button {
  background: #3f2a1f;
  color: #fff8f2;
}

.secondary-button {
  background: #ead7c5;
  color: #4a3022;
}

.error-card {
  text-align: center;
}

.skeleton {
  animation: pulse 1.2s ease-in-out infinite;
}

@keyframes pulse {
  0% {
    opacity: 0.65;
  }
  50% {
    opacity: 1;
  }
  100% {
    opacity: 0.65;
  }
}
