.page {
  min-height: 100vh;
  padding: 32rpx;
  background: linear-gradient(180deg, #f9f3ec 0%, #efe6dc 100%);
}

.hero-stage {
  position: relative;
}

.hero-error {
  padding: 40rpx 36rpx;
  border-radius: 36rpx;
  background: linear-gradient(135deg, #34231a 0%, #5a3c2f 100%);
  color: #fff7f0;
  box-shadow: 0 26rpx 46rpx rgba(52, 35, 26, 0.18);
}

.hero-error-title {
  display: block;
  font-size: 50rpx;
  font-weight: 700;
}

.hero-error-subtitle {
  display: block;
  margin-top: 18rpx;
  line-height: 1.8;
  color: rgba(255, 247, 240, 0.82);
}

.primary-button {
  background: #f0d7bf;
  color: #382015;
  font-weight: 600;
}

.primary-button::after,
.hero-chip::after {
  border: 0;
}

.banner-swiper {
  height: 600rpx;
  border-radius: 40rpx;
  overflow: hidden;
  background: rgba(255, 255, 255, 0.72);
  box-shadow: 0 30rpx 54rpx rgba(90, 60, 47, 0.14);
}

.banner-image-frame,
.banner-empty {
  width: 100%;
  height: 100%;
}

.banner-image-frame {
  overflow: hidden;
}

.banner-image {
  width: 100%;
  height: auto;
  background: rgba(255, 250, 245, 0.92);
}

.banner-empty {
  display: flex;
  align-items: center;
  justify-content: center;
  color: #9a7f6f;
  font-size: 26rpx;
}

.hero-summary {
  margin: 20rpx 10rpx 0;
  padding: 24rpx 26rpx 20rpx;
  border-radius: 28rpx;
  background: rgba(255, 250, 245, 0.9);
  box-shadow: 0 10rpx 18rpx rgba(90, 60, 47, 0.06);
}

.hero-summary-skeleton {
  min-height: 190rpx;
}

.eyebrow {
  display: block;
  font-size: 20rpx;
  letter-spacing: 5rpx;
  color: #a38a78;
}

.hero-title {
  display: block;
  margin-top: 10rpx;
  font-size: 34rpx;
  font-weight: 700;
  color: #382015;
}

.hero-subtitle {
  display: block;
  margin-top: 12rpx;
  line-height: 1.65;
  color: #735b4d;
  font-size: 24rpx;
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
  overflow: hidden;
}

.hero-actions {
  margin-top: 18rpx;
  display: flex;
  align-items: center;
  gap: 16rpx;
}

.hero-chip {
  margin: 0;
  padding: 0 24rpx;
  min-width: 180rpx;
  height: 64rpx;
  line-height: 64rpx;
  border-radius: 999rpx;
  font-size: 22rpx;
  font-weight: 600;
}

.hero-chip-primary {
  background: #3e2a1f;
  color: #fff7f0;
}

.hero-chip-secondary {
  background: rgba(240, 223, 209, 0.72);
  color: #6f5240;
}

.panel {
  margin-top: 24rpx;
  padding: 28rpx;
  border-radius: 28rpx;
  background: rgba(255, 255, 255, 0.84);
  box-shadow: 0 18rpx 32rpx rgba(90, 60, 47, 0.08);
}

.panel-header {
  display: flex;
  align-items: baseline;
  justify-content: space-between;
}

.panel-title {
  font-size: 34rpx;
  font-weight: 700;
  color: #382015;
}

.panel-caption {
  font-size: 22rpx;
  color: #9a7f6f;
}

.product-list {
  margin-top: 20rpx;
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 20rpx;
}

.product-column {
  flex: 1;
  min-width: 0;
  display: flex;
  flex-direction: column;
  gap: 20rpx;
}

.product-card {
  overflow: hidden;
  border-radius: 24rpx;
  background: #fff9f5;
  box-shadow: 0 14rpx 24rpx rgba(90, 60, 47, 0.08);
}

.product-image,
.product-placeholder {
  width: 100%;
}

.product-placeholder {
  min-height: 240rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  color: #a68b7a;
  background: #f3e8de;
}

.product-body {
  padding: 22rpx;
}

.product-name {
  display: block;
  font-size: 30rpx;
  font-weight: 600;
  color: #352219;
}

.tag-row {
  margin-top: 14rpx;
  display: flex;
  flex-wrap: wrap;
  gap: 12rpx;
}

.tag {
  padding: 8rpx 16rpx;
  border-radius: 999px;
  background: #f1dfd1;
  color: #7a5947;
  font-size: 22rpx;
}

.empty-text {
  margin-top: 24rpx;
  color: #8f7362;
  text-align: center;
}

.skeleton {
  animation: pulse 1.2s ease-in-out infinite;
  background: rgba(255, 255, 255, 0.7);
}

@keyframes pulse {
  0% {
    opacity: 0.65;
  }
  50% {
    opacity: 1;
  }
  100% {
    opacity: 0.65;
  }
}
