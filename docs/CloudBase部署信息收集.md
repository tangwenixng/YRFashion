# CloudBase 部署信息收集

版本：v0.1  
日期：2026-03-14

本文用于收集将 `backend` 管理后端部署到 CloudBase / 云托管所需的关键信息。  
你可以直接在本文件对应位置补充答案，后续我会基于这份文档继续修改代码和整理部署步骤。

## 1. 本次部署目标

请确认本次希望达到的目标：

- [ ] 先完成“后端可部署到云托管”的代码改造
- [ ] 完成“后端 + MySQL + 云存储”的联通
- [ ] 完成“小程序真实登录（code2Session）”接入
- [ ] 完成“后台前端”生产部署方案
- [ ] 直接按正式生产环境配置
- [ ] 先做可运行的测试 / 预发布版本

补充说明：

-

## 2. CloudBase 环境信息

### 2.1 环境基础信息

- CloudBase 环境 ID：
- CloudBase 环境名称：
- 所在地域：
- 当前是否已创建环境：`是 / 否`

### 2.2 云托管信息

- 当前是否已开通云托管：`是 / 否`
- 后端准备使用：
  - [ ] CloudBase 云托管代码部署
  - [ ] CloudBase 云托管镜像部署
- 当前是否已创建后端服务：`是 / 否`
- 如果已创建，服务名称：

### 2.3 环境变量管理方式

- 计划在 CloudBase 控制台中配置环境变量：`是 / 否`
- 是否接受将所有生产密钥都放入环境变量：`是 / 否`
- 是否已有统一命名规范：`是 / 否`

补充说明：

-

## 3. 数据库信息

### 3.1 数据库选型

请选择本项目正式环境数据库方案：

- [ ] CloudBase / 腾讯云托管 MySQL
- [ ] 已有腾讯云 MySQL
- [ ] 其他 MySQL

### 3.2 数据库现状

- 当前数据库是否已经创建：`是 / 否`
- 当前是否已有库名：
- 当前是否已有账号：
- 当前是否已有密码：
- 当前是否已有连接地址：
- 当前是否已有端口：

### 3.3 连接方式

- 计划提供的 `DATABASE_URL`：
- 是否要求内网连接优先：`是 / 否`
- 是否需要连接池特殊限制：`是 / 否`

补充说明：

-

## 4. 文件存储 / 图片存储信息

### 4.1 存储方案

请选择商品图片和媒体文件存储方案：

- [ ] CloudBase 云存储
- [ ] COS
- [ ] 其他对象存储

### 4.2 访问策略

- 图片是否允许公网直接访问：`是 / 否`
- 是否要接入 CDN：`是 / 否`
- 最终图片访问域名：
- 是否需要保留私有读能力：`是 / 否`

### 4.3 存储桶 / 路径规划

- 存储桶名称：
- 环境前缀规划（如 `dev/`、`prod/`）：
- 图片路径是否按商品分目录：`是 / 否`

补充说明：

-

## 5. 域名与备案信息

### 5.1 API 域名

- API 正式域名（如 `api.xxx.com`）：
- 当前状态：
  - [ ] 未购买
  - [ ] 已购买
  - [ ] 已备案
  - [ ] 已可解析
  - [ ] 已可用于 HTTPS

### 5.2 后台域名

- 后台正式域名（如 `admin.xxx.com`）：
- 当前状态：
  - [ ] 未购买
  - [ ] 已购买
  - [ ] 已备案
  - [ ] 已可解析
  - [ ] 已可用于 HTTPS

### 5.3 备案情况

- 小程序备案是否已完成：`是 / 否`
- 域名备案是否已完成：`是 / 否`
- 如果未完成，当前卡在哪一步：

补充说明：

-

## 6. 小程序正式登录信息

当前仓库中的小程序登录仍是 mock 逻辑，如要切生产，需补微信真实登录配置。

- 小程序 `AppID`：
- 小程序 `AppSecret`：
- 当前是否允许我接入真实 `code2Session`：`是 / 否`
- 当前是否已有正式小程序主体：`是 / 否`
- 当前是否已在微信后台配置服务器域名：`是 / 否`

补充说明：

-

## 7. 后台管理账号与安全配置

- 生产环境 JWT `SECRET_KEY`：
- 初始管理员用户名：
- 初始管理员密码：
- 管理员显示名：

是否需要额外限制：

- [ ] 允许的前端来源域名白名单
- [ ] 更严格的密码规则
- [ ] IP 限制
- [ ] 登录限流

如果需要，请填写：

-

## 8. 前端与小程序访问方式

### 8.1 后台前端

- 后台前端是否也准备部署到 CloudBase：`是 / 否`
- 如果是，是否使用 CloudBase 静态网站托管：`是 / 否`
- 后台前端生产访问域名：

### 8.2 小程序

- 小程序生产环境 API 地址：
- 小程序开发环境 API 地址：
- 小程序是否需要与测试环境隔离：`是 / 否`

补充说明：

-

## 9. 你希望我先做的代码改造范围

请选择我下一步直接动手的范围：

- [ ] 只改后端，使其支持 MySQL + 云存储 + 环境变量
- [ ] 后端 + Dockerfile + 部署说明
- [ ] 后端 + 小程序真实登录
- [ ] 后端 + 前端 API 域名适配
- [ ] 一次性把后端部署相关改造做完

补充说明：

-

## 10. 备注

你可以把不方便公开的真实密钥先留空，只先填写“是否已有”与“后续提供方式”。  
如果你愿意，我拿到这份文档的回复后，可以直接开始改代码并顺带补一份 CloudBase 实际部署操作清单。
